<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Create Product</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
        background: linear-gradient(135deg, #e0f7fa, #fff);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .card-form {
      max-width: 800px;
      margin: 50px auto;
      background: white;
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }
    .card-form h2 {
      text-align: center;
      margin-bottom: 30px;
      color: #fa4444;
      font-weight: 600;
    }
    .form-label {
      font-weight: 500;
      color: #333;
    }
    .btn-submit {
      background-color: #e75cd0;
      border: none;
      padding: 12px;
      font-size: 1.1rem;
      font-weight: 500;
      transition: all 0.3s;
    }
    .btn-submit:hover {
      background-color: #da53af;
    }
    .form-control:focus {
      box-shadow: 0 0 0 0.2rem rgba(108, 92, 231, 0.25);
      border-color: #6c5ce7;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card-form">
      <h2>🛍️ Add New Product</h2>
      <form action="/products/create" method="POST">
        <div class="row g-4">
          <div class="col-md-6">
            <label for="name" class="form-label">Product Name</label>
            <input type="text" class="form-control" id="name" name="name" placeholder="e.g., Modern Lamp" required>
          </div>
          <div class="col-md-6">
            <label for="price" class="form-label">Price ($)</label>
            <input type="number" class="form-control" id="price" name="price" placeholder="e.g., 59.99" required>
          </div>
          <div class="col-12">
            <label for="description" class="form-label">Description</label>
            <textarea class="form-control" id="description" name="description" rows="3" placeholder="Brief description..." required></textarea>
          </div>
          <div class="col-12 d-grid mt-3">
            <button type="submit" class="btn btn-submit">➕ Create Product</button>
          </div>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
